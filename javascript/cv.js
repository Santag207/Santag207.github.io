// CV specific logic for NASA theme
// Currently, animations are handled globally by utils.js IntersectionObserver
// Any future CV-specific interactions can be added here

document.addEventListener('DOMContentLoaded', () => {
    // If we need to re-run any logic after component load
});
